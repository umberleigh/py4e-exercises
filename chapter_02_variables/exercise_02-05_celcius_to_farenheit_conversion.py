# Prompt the user for a Celsius temperature, convert the temperature to
# Fahrenheit, and print out the converted temperature
temp_in_c = input('Enter temperature in Celsius: ')
temp_in_faren = (float(temp_in_c) * 1.8) + 32
print(temp_in_faren)
